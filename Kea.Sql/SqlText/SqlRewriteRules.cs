using System;
using KeaSql.ExprRewrite;

namespace KeaSql.SqlText
{
    public static class SqlRewriteRules
    {
        public static string ExprToSql<T>(T expr) => throw new ArgumentException("Esta función no se puede llamar directamente");

      
    }
}
